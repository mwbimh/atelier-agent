[package]
license = "Apache-2.0"
name = "atelier-runtime-events"
version = "0.1.0"
edition.workspace = true
description = "Local per-session runtime event types and JSONL writer"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
strum = { workspace = true }
tracing = { workspace = true }
chrono = { workspace = true }
[features]
default-bazel = []


[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
