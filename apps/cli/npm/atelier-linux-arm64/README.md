# @atelier/atelier-linux-arm64 packaging scaffold

This directory is development-only packaging scaffolding for a possible future
Linux ARM64 Atelier CLI package. It has not been published to npm and must not
be presented as an available installation method.

The manifest is marked `private: true` to prevent accidental publication.
